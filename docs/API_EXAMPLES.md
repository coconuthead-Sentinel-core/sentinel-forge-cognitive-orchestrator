# API Examples Template

## Purpose
Use this file to store request and response examples for the project API after `docs/API.md` is defined.

## Example Format
### [Endpoint name]
Request:
```json
{
  "[field]": "[value]"
}
```

Response:
```json
{
  "[field]": "[value]"
}
```

## Notes
- Keep examples short and valid.
- Use redacted or synthetic data only.
- Remove sections that are not needed.
